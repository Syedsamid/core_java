import java.io.*;
import java.lang.*;

class bus 
{
	public static void main (String args[])throws IOException
	{
		int cost
		double km
		double pvt,gov
		BufferedReader ob=new BufferedReader(new InputStreamReader(System.in));

		case 1:
		if (bus==1)
		{
		System.out.println("\n\t private of bus=");
		cost=Integer.parseDouble(ob.readline())
		}
		else if(bus==2)
		{
		System.out.println("\n\t goverment bus=");
		km=Integer.parseDouble(ob.readline())
		}
		else
		{
			System.out.println("\n\t DO you want to continue=");
	        Integer.parseDouble(ob.readline())
			
			
		
		 
		
		
		
		